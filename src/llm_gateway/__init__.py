"""llm-gateway — a token-frugal, self-hostable LLM gateway / proxy.

An OpenAI-compatible reverse proxy with honest token & cost accounting,
virtual API keys, budgets, response caching, and built-in JTF (JSON Token
Format) savings observability.
"""

__version__ = "0.1.0"
